<h1>מה חושבים עליכם - הכנסת ההערות</h1>
<p>הכניסו את ההערות למשתמשים כשכל הערה בשורה נפרדת</p>

<%form_tag :action => :save_comments do %>
<table>
	<% for user in @users.reject{|u| u.name == session[:name] or u.participates == 0} do %>
	  <tr>
		<td> <%=h user.name %>: </td>
		<td> <%= text_area_tag "user_" + user.name, user.comments_string(session[:name]), :size => "80x4" %> </td>
	  </tr>
	<% end %>
</table>

<center>
  <%= submit_tag "עדכן" %>
  <%= link_to "חזרה", :action => "index" %>
  </center>
<% end %>